import {sqliteTable,text,integer,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
import {sql} from 'drizzle-orm';
export const users=sqliteTable('users',{id:text('id').primaryKey(),email:text('email').notNull().unique(),name:text('name').notNull(),password:text('password').notNull(),salt:text('salt').notNull(),role:text('role').notNull(),settings:text('settings').notNull().default('{}'),revision:integer('revision').notNull().default(1)});
export const sessions=sqliteTable('sessions',{token:text('token').primaryKey(),userId:text('user_id').notNull().references(()=>users.id,{onDelete:'cascade'}),expires:integer('expires').notNull()});
export const records=sqliteTable('records',{id:text('id').primaryKey(),owner:text('owner').notNull().references(()=>users.id,{onDelete:'cascade'}),kind:text('kind').notNull(),date:text('date').notNull(),payload:text('payload').notNull(),revision:integer('revision').notNull().default(1),updated:text('updated').notNull()},t=>[index('records_owner_date').on(t.owner,t.date),uniqueIndex('one_daily_record').on(t.owner,t.kind,t.date).where(sql.raw("kind IN ('sleep','meal')"))]);
export const habits=sqliteTable('habits',{id:text('id').primaryKey(),owner:text('owner').notNull().references(()=>users.id,{onDelete:'cascade'}),payload:text('payload').notNull(),revision:integer('revision').notNull().default(1)},t=>[index('habits_owner').on(t.owner)]);
export const shares=sqliteTable('shares',{id:text('id').primaryKey(),recordId:text('record_id').notNull().references(()=>records.id,{onDelete:'cascade'}),viewer:text('viewer').notNull().references(()=>users.id,{onDelete:'cascade'})},t=>[uniqueIndex('shares_record_viewer').on(t.recordId,t.viewer),index('shares_viewer').on(t.viewer)]);
export const files=sqliteTable('files',{id:text('id').primaryKey(),recordId:text('record_id').notNull().references(()=>records.id,{onDelete:'cascade'}),owner:text('owner').notNull(),name:text('name').notNull(),mime:text('mime').notNull(),size:integer('size').notNull()},t=>[index('files_record').on(t.recordId)]);
export const invites=sqliteTable('invites',{token:text('token').primaryKey(),email:text('email').notNull().unique(),expires:integer('expires').notNull(),used:integer('used').notNull().default(0)});
export const attempts=sqliteTable('attempts',{key:text('key').primaryKey(),count:integer('count').notNull(),until:integer('until').notNull()});
