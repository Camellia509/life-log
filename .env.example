# Browser build. Stage 2 local development points directly to the Worker.
VITE_LIFE_API_BASE_URL=http://localhost:8787/api/life

# Worker production setup placeholders. Configure actual values outside Git.
# Do not put actual passwords or setup codes in this file.
ALLOWED_ORIGINS=
MINT_OWNER_EMAIL=
MINT_SETUP_TOKEN=
